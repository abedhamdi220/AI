meta {
  name: [GET] api/coupons
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/coupons
  body: none
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{auth_token}}
}

settings {
  encodeUrl: true
}
