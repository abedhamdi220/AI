meta {
  name: [DELETE] api/designs/{id}
  type: http
  seq: 7
}

delete {
  url: {{base_url}}/api/designs/:id
  body: none
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{auth_token}}
}

settings {
  encodeUrl: true
}
