meta {
  name: [PUT] api/admin/orders/{id}/status
  type: http
  seq: 1
}

put {
  url: {{base_url}}/api/admin/orders/:id/status
  body: none
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{auth_token}}
}

settings {
  encodeUrl: true
}
