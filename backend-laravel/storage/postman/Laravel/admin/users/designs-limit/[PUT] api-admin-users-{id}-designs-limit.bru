meta {
  name: [PUT] api/admin/users/{id}/designs-limit
  type: http
  seq: 1
}

put {
  url: {{base_url}}/api/admin/users/:id/designs-limit
  body: none
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{auth_token}}
}

settings {
  encodeUrl: true
}
