meta {
  name: [DELETE] api/admin/designs/{id}
  type: http
  seq: 2
}

delete {
  url: {{base_url}}/api/admin/designs/:id
  body: none
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{auth_token}}
}

settings {
  encodeUrl: true
}
