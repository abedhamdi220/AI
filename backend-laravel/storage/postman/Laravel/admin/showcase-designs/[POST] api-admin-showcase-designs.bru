meta {
  name: [POST] api/admin/showcase-designs
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/admin/showcase-designs
  body: none
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{auth_token}}
}

settings {
  encodeUrl: true
}
