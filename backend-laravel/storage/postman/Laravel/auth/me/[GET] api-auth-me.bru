meta {
  name: [GET] api/auth/me
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/auth/me
  body: none
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{auth_token}}
}

settings {
  encodeUrl: true
}
