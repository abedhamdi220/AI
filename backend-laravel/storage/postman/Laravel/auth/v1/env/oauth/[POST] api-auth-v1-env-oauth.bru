meta {
  name: [POST] api/auth/v1/env/oauth
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/auth/v1/env/oauth
  body: none
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{auth_token}}
}

settings {
  encodeUrl: true
}
